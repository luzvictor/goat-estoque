import LoginPageClient from "./_client";

export default function LoginPage() {
  // Este componente do lado do servidor simplesmente renderiza o componente do lado do cliente.
  return <LoginPageClient />;
}
